# 代码规范（CODE STANDARDS）

> 本文件是**强制规范**，由 `AGENTS.md` / `CLAUDE.md` 引用。写任何代码前先过一遍本清单。

## 一、设计原则（高内聚低耦合之外）

| 原则 | 强制要求 |
|---|---|
| **SOLID** | 单一职责（一个函数/模块只做一件事，`main()` 过长必须拆入口函数）；开闭（加能力改配置表/策略，不改主流程）；依赖倒置（面向接口不面向实现） |
| **DRY** | 同一逻辑只写一处；**多平台/多仓库的重复代码必须抽公共核心 + 差异配置，禁止手动复制漂移**（当前 timem/timemspace 双脚本必须以此方式维护） |
| **KISS / YAGNI** | 不做用不到的功能；优先标准库，零不必要依赖 |
| **最小惊讶** | 行为符合直觉：退出码语义化（0 成功 / 1 失败 / 2 参数错误）、日志统一 `[信息]/[完成]/[错误]/[验证]/[升级]/[卸载]/[重启]` 前缀 |
| **幂等性** | 所有安装/更新/卸载可重复执行且结果一致（`applyPatch`/`removeEntry` 必须幂等） |
| **失败安全** | 失败时不破坏现状：验证不通过不写入、kill/重启失败回退到打印手动命令、文件写入必须原子化（临时文件 + rename） |
| **优雅降级** | 网络/TLS/权限受限时给可读提示（含解决方案），不允许裸崩溃 |
| **CQS 命令查询分离** | 只读操作（`--verify`/`--dry-run`/`--check-update`）与写操作（安装/卸载/升级）严格分离 |
| **最小权限** | 只访问/修改自己负责的文件；危险操作（杀进程、删目录）必须显式确认 |

## 二、编码规范

| 维度 | 强制要求 |
|---|---|
| **命名** | 语义化、统一风格（camelCase 函数/变量、SCREAMING_SNAKE 常量）；魔法值必须抽常量（如 `ENTRY_ID`/`DEFAULTS`/`SKILLS`）；避免 `r1`/`a1` 式无意义短名 |
| **注释** | 写"为什么"与失败路径，不写"是什么"；函数头注明参数、返回、可能失败的原因 |
| **错误处理** | 不吞异常、不 catch 后静默；统一 `main().catch` 出口 + 退出码；输入校验 fail-fast 并给出可操作错误信息 |
| **输入验证** | 参数格式/边界/枚举校验（如 `serverName` 正则、skill 白名单）；对配置文件解析要容忍并提示（不要静默匹配失败） |
| **资源管理** | 所有网络请求必须设超时；spawn 子进程 `detached+unref` 不挂起；读文件处理不存在/编码异常 |
| **安全** | 密钥一律环境变量或交互输入，**禁止** `--key` 明文进命令历史（文档同步说明）；日志不回显密钥；落盘敏感文件提示权限 |
| **可测试性** | 纯函数（解析、补丁、比较、构建）与副作用（IO、子进程）分离；核心逻辑必须有 `node:test` 单测 |

## 三、工程实践

| 实践 | 强制要求 |
|---|---|
| **版本管理** | semver + tag（`vX.Y.Z`）；commit message 用前缀（feat/fix/docs/chore/refactor/test） |
| **变更记录** | 每次发版更新 `CHANGELOG.md`（Keep a Changelog 格式），与 tag 一一对应 |
| **测试** | 核心纯函数必须有单测；`node --check` 语法检查纳入流程 |
| **CI** | GitHub Actions：push/PR 跑 `node --check` + `node:test` + tag 一致性校验 |
| **文档** | 功能变更必须同步 README（选项表、示例、维护命令）；行为变化要写"为什么" |
| **发布流程** | 发版 = 改 version + 更新 CHANGELOG + 打 tag + push（可脚本化 `npm version`） |

## 四、本项目硬性规则（Node 脚本类）

1. **零依赖**：只用 Node 内置模块（fs/path/https/child_process/readline/url），新依赖必须论证
2. **跨平台**：Windows 7+/Linux/macOS 行为一致；平台差异用 `process.platform` 分支，绝不假设
3. **配置文件**：`cordis.patch.yml` 修改必须幂等 + 原子写 + 保持原换行符与 UTF-8
4. **skill 安装**：内置 skill 目录结构固定 `skills/<name>/SKILL.md`；安装幂等、可覆盖（默认询问）、可卸载
5. **重启引导**：自动重启只能终止监听 DSH 端口的进程，任何失败回退到打印手动命令

## 五、禁止事项

- ❌ 不写注释掉的死代码（删掉，git 有历史）
- ❌ 不在代码里硬编码密钥/令牌/token
- ❌ 不 catch 后 `console.log` 了事（要么处理要么向上抛）
- ❌ 不同步 README / CHANGELOG 就合并功能
- ❌ 复制粘贴另一份脚本再改参数（必须抽公共核心）
